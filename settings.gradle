pluginManagement {
    repositories {
        maven {
            name = 'Fabric'
            url = 'https://maven.fabricmc.net/'
        }
        gradlePluginPortal()
    }
}

// Pin the root project name so it never derives from the containing directory.
rootProject.name = 'touhoulittlemaid-fabric'
