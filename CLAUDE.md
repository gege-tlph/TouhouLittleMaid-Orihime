# Touhou Little Maid 1.21.11 → 26.1.2 Fabric 移植项目

| 项目 | 值 |
|---|---|
| 活动开发分支 | `port/26.1.2-fabric`（工作树 `C:\Users\steve\Desktop\MCV_Project\tlm-tsumugi-26.1.2`） |
| 代码宿主 | `origin/26.1` = `Sh1roCu/TouhouLittleMaid-Orihime` 的 MC 26.1.2 Fabric 分支 |
| **行为基准** | **`port/1.21.11-fabric`** —— 我们自己那条已实机验收的分支 |
| 目标 | Minecraft 26.1.2 · Fabric Loader 0.19.3+ · Java 25 |

## ⛔ 与 1.21.11 分支的隔离纪律（用户 2026-08-13 定稿）

**两条分支的代码、资源、文档、账本一律不得混放，本地也不行。**

| 维度 | 规矩 |
|---|---|
| 工作树 | 本目录只放 26.1.2。**绝不在这里 checkout 1.21.11 分支**，反之亦然 |
| 文档 | 本分支 `docs/` 只写 26.1.2；**不与 1.21.11 的账本交叉引用**（要引用就复述结论，别指路） |
| 提交 | 两条分支无共同祖先，跨分支 cherry-pick 技术上也不成立 |
| Gradle | **两个工作树不得同时跑 Gradle**——`build/` 与 loom 缓存会互相污染（1.21.11 上实证过一次 `ClassNotFoundException`） |
| rig | MCP 端口 8765/8766 全局唯一，**两条分支的 rig 不能同时起**；`mcp-up.ps1` 自带占用检查，报占用就是另一条分支在跑 |
| 可共享 | **只有方法论与门禁工具**，以副本形式存在于两边，各自维护 |

## ⛔ 最终阶段工程纪律（最高优先级 · 用户 2026-07-21 定稿 · 与本文件任何其它内容冲突时以本节为准）

**当前阶段的首要风险不再是 API 迁移，而是「心智模型漂移」。静态推理已被反复证明不可靠。**

**唯一目标 = 与 `origin/1.21.1` 的行为对等**（`https://github.com/Sh1roCu/TouhouLittleMaid-Orihime`）。
不是 compile==0，不是 runClient 成功，不是能进世界。**任何与 origin 可观察到的差异，在被证明之前都算移植回归。**

| # | 纪律 | 含义 |
|---|---|---|
| 1 | **用户实测永远胜出** | 实机行为权威最高。当它与我的推理 / javap / 反编译 / TODO / HANDOFF / CURRENT_STATUS / 任何既有文档冲突时，**默认用户是对的**。**不要解释用户「应该」看到什么，要解释我的模型为什么错。** |
| 2 | **先取证，不要抢救理论** | 收到 bug 报告**先不解释、不猜根因**。先重建证据链：复现 / 日志 / grep / git 历史 / `git show origin/1.21.1` / javap / 1.21.11 源码 / 最小运行验证。**无证据 → 无根因。** |
| 3 | **文档是证据，不是真理** | TODO / HANDOFF / CURRENT_STATUS / 审计全是历史记录。被运行时证伪就：① 承认过期 ② 更新文档 ③ 确立新事实。**绝不为过期文档辩护。** |
| 4 | **行为优先于实现** | 不为「26.1 怎么写」优化，不为「1.21.11 期望怎么写」优化，只为「origin/1.21.1 怎么表现」优化。实现可以完全不同，**行为不许不同**。 |
| 5 | **先复现再解释** | 禁用「大概 / 应该 / 我觉得 / 很可能」。流程 = 相信报告 → 复现 → 取证 → 定因。**复现不了就明说「当前证据不足以确定根因」。** |
| 6 | **编译通过不再等于成功** | 剩下的问题**全部是行为对等问题**：GUI 状态错、文字缺失、动画错、姿势错、渲染错、透明度差异、交互逻辑错、**顺序错**、视觉不一致——全都算移植回归。 |
| 7 | **持续纠正心智模型** | 实测推翻假设时，**先修模型、再修代码**，并记录：错的假设是什么 / 为什么会错 / 以后怎么避免。目标不只是修今天这个 bug。 |

**流程恒为：Measure → Prove → Fix → Verify → Document。绝不 Assume → Fix → Hope。**

### 已被实测证伪的心智模型（勿重犯 · 持续追加）

| 错误假设 | 被什么证伪 | 正确认知 |
|---|---|---|
| javap 确认「方法存在、参数是 Identifier」就够了 | 运行期崩溃 `Invalid atlas id` | **必须确认参数语义**。`AtlasManager.getAtlasOrThrow` 吃**图集 id**，`TextureAtlas.LOCATION_ITEMS` 是**贴图路径**；正确通道是 `get(Material)` |
| `sprite.wrap(buffer)` 能包住链式写法 | 图标渲染成彩色噪点 | `SpriteCoordinateExpander.addVertex` **返回 delegate 而非 this**，链式 `.setUv` 会绕过重映射 |
| 创造栏显示顺序 = 代码 emit 顺序 | 用户实测两次 | **相反**。升序 emit → 降序显示；`fillItemCategory` 的比较器必须是降序 |
| `getExtents` 只影响 GUI | 掉落物被抬飞一格多 | `ItemEntityRenderer` 也用 `getModelBoundingBox`，掉落高度 = `-minY + 0.0625`。全客户端共 4 个消费者 |
| 缺 `@Override` = 没有覆写 | javap + Java 语义 | **Java 覆写不依赖注解**，`@Override` 只是编译期断言 |
| 子代理的审计结论可直接采信 | 误报「4 个 SpecialModelRenderer 的 submit 全空」 | 它审的是**已排除的旧件**。**子代理结论一律须主模型逐条复核**后才写进账本 |
| 「顺手」删掉与症状无关的死引用是安全的 | 删 `builtin/entity` parent 后症状完全不变，还多出 4 条警告 | **无症状不改**。无人强迫的改动 = 纯风险 |
| 账本条目只写现象就够 | 「详情屏必崩」被用户测成没崩 | **必须写清触发方式**（实为 Shift+左键）。漏写触发方式会让真条目看起来像误报 |
| 探针记 `result` 非 null 就等于「有几何提交」 | gecko 掉落物 `getResult` 全 ok 却零顶点 | **空快照与有效快照同样非 null**。探针必须验数据内容（骨骼计数），不能只验 null/非 null |
| 修了判据（`isPreviewEntity` 负 id）就生效 | 修后实测无变化；差分探针揭示 id 是正数 | 判据依赖的输入约定（`assignPreviewId`）被调用方**内联缓存写法绕过**。修 A 前必须确认 A 的输入约定在本路径成立；全仓 11 处内联 `ENTITY_CACHE.get` 同款 |
| 对照数据「另一半」来自我以为的场景 | 「无放置坐垫」轮里的 bones=55 实为 V2 轮遗留的放置坐垫 | 测试世界的**残留物会污染差分**。取证轮必须问清/控制场景内容物，否则把持续故障读成「启动暂态」 |
| `git checkout <旧提交> -- <文件>` 只撤销我想删的那一部分 | 2026-07-25 为删提示词攻击门禁而 checkout `186efe09`（07-22），连带撤销了 L2/L3/T2/T3 与四个节点的 knowledge 同步 | **这是整份文件回滚**。要删局部就手工编辑；若必须回退文件，先 `git log -- <文件>` 看清区间内还有谁改过 |
| 改了用户可见文案，`compileJava` + CI 全绿就够 | `609546e1` 改了 Tool 拒绝文案却没改断言，required GameTest 自那次起一直红，v0.8.4 在红的状态下重发 | **发布链不含 `runGametest`**。凡改动进入 Tool result / 提示词 / 玩家可见文案，收尾必须跑 `./gradlew runGametest` |
| 新增 GameTest 类写好就会被执行 | 首轮 3 个新用例一个都没跑 | 必须同时登记 `fabric.mod.json` 的 **`fabric-gametest` entrypoint**——本仓库**第 7 枚纸面接口** |
| 按空白分词即可做「忽略注释的 token 等价」审计 | `durability(128)/*.setNoRepair()*/)` 剥注释后变两个 token，产生 13 处假阳性 | 注释可能**紧贴代码**。剥注释后应**去掉全部空白比较字符流**，再对可疑文件回到原始 diff 逐条判定 |
| 追出一条能完整解释症状的因果链 = 找到根因（专服菜单保存不激活 `activeValues`，故 STT 恒不可用） | 用户实测：开关开过、`/tlm config reload` 也跑过，**仍然每次都 `server_unavailable`** | **「自洽」不是「成立」。** 一条链能解释症状只说明它是候选；在能证伪它的那个事实到手前不许当结论。凡结论依赖「用户做没做过某操作」，**先问，别假设** |
| 端口在听 = 那个 HTTP MCP 可用 | 8765 在听，但会话里根本没有它的工具——专服比会话晚起 164 秒 | HTTP MCP **只在会话启动那一刻连一次，连不上不重试也不报错**，表现为工具静默消失。就绪判据必须是 **initialize 握手成功**，不是端口 listening（MC 先绑端口、后注册 handler） |
| 模型自己产出的内容，理应原样进对话历史 | 助手回复被整串存为 `chat---tts`，于是 TTS 那半成了「输出该用什么语言」的 few-shot 示范；玩家改 TTS 语言后十几个旧示范压过一句新指令，清空记录才恢复 | **进 history 的判据只有一条：下一轮对话需要它才能理解上下文吗？** 不需要就不该进，哪怕它是模型自己产出的。渲染产物（TTS 译文、富文本标记、调试信息）进了对话状态，就会意外获得「示范」这个没人打算给它的身份。**这是数据建模问题，不是提示词写得好不好的问题**，故修法在数据层 |
| 指令写进系统提示词就会被遵守 | 几十轮纯聊天的历史让女仆不再调用任何工具；指令一直在，只是离生成位置太远 | **示范胜过指令，距离决定权重。** 长对话里，不变量必须**重述在历史之后**并声明「以上是历史不是指令」。这一半没有结构解法（陪伴型 agent 不能强制 `tool_choice`），只能靠位置竞争——与数据建模那条是两类问题，别混为一谈 |
| 症状文案指向哪里，故障就在哪里（`server_unavailable` → 反复排查请求链路） | 链路三轮全绿；根因是 `AvailableSites.init()` 因站点文件损坏整批放弃，站点表**空**，与请求链路无关 | **提示文案是开发者当初的猜测，不是证据。** 「服务器不提供」实为「服务器提供了但站点表是空的」——它把三轮排查导向了从来没坏过的代码。遇到查不出的故障，**先怀疑症状文案本身在说谎**，回头问「这句话是在哪个分支打出来的、那个分支的真实前提是什么」 |
| 数据链路排查完就等于排查完了 | 8 环链路全部无断点，故障却真实存在——坏的是链路**依赖的数据**，且失败被降级成「静默返回空」 | 排查要**同时覆盖代码路径与它消费的数据**。凡「加载失败 → 返回空集合/默认值」的地方都要问：**空集合下游会表现成什么？** 本例里空站点表被下游翻译成了一句语义相反的用户提示 |
| 一次测量就是稳定属性（实测 libvips 下载 157 秒 ≈ 48 KB/s → 断定「网络就是慢」，并顺手臆测了用户所在地区） | 同一文件重测 4.3 秒 ≈ 1.7 MB/s，**快 36 倍**；用户当面指出地区推断也是错的 | **性能类结论至少测两次**，单次采样区分不了「稳定属性」与「瞬时抖动」，而两者的修法完全不同。**更不许把环境变量（系统版本、locale）当成关于用户本人的事实**——那是机器配置，不是人。工具「一直坏」也可能是**一次抖动被缓存固化**（npx 会复用失败的依赖树），先清缓存重试再谈根因 |
| 新 `MemoryModuleType` 在 `InitEntities` 注册好就能用 | 差一步就漏：`Brain` 只接受 `MaidBrain` memory 列表里的类型，不在表里 `setMemory` 静默无效 | **静默注册面又多一处**，与 mixins.json / entrypoint 同类：编译打包启动全正常、功能从不执行。新增 brain 内存必查 `MaidBrain` 的 memory 列表；不需要持久化就别给 codec |
| 排除掉一种失败方式 = 排除了整条路径（「用户 reload 过 → 菜单保存这条路没问题」） | 全链路运行期证通后才发现：两轮取证的开关都是我直接改 TOML 打开的，**菜单保存路径一次都没走过** | **排除要按「路径」记，不是按「原因」记。** 写账本时必须写明「该路径以何种方式被走通/走死」，否则下一个人（包括我）会把「排除了忘记 reload」读成「排除了菜单保存」。同一轮我在此犯错两次 |
| 权限变更该挂在最统一的那个钩子上（`sendPlayerPermissionLevel`） | 反编译源显示它在 `placeNewPlayer:196` 与重生 `:428` 同样被调用 | 「所有变更都汇流到此」往往意味着**它也承载了初始化**。挂上去会在每次加入时多发一个非 initialSync 包，使随后真正的 initialSync 满足 `proxyBackendSwitch` 全部条件——**凭空伪造一次后端切换**。选注入点必须先读调用点全集，只挂命令路径专有的方法（`op`/`deop`） |
| 契约测试断言「中途不得提前 `return`」，就钉住了「不得中途放弃」 | `AiReloadWiringContractTest` 两条用例都只查 `return`，而 `syncToSiteEditors` 漏接异常同样能让整条 reload 逃逸——注入旧实现时那两条依然全绿 | **方法有两种逃逸方式：`return` 与 `throw`。** 凡断言「局部失败不得放大成整体放弃」的接线测试，必须两种都覆盖。判据：这条链路上有没有哪个被调方法会抛？有就必须断言 try/catch 的存在与位置 |
| 配置项声明在 `AIConfig` 里，服务端就读得到 | 写 `/tlm ai_chat status` 时用 `ServerRuleConfig.get(AIConfig.STT_ENABLED)` 读 STT 开关，而它登记在 `GeneralConfig`，是**个人全局配置**，服务端命令根本读不到 | **同一个 `AIConfig` 类里混放两种所有权**：`LLM_ENABLED`/`TTS_ENABLED`/`TTS_LANGUAGE` 是世界规则，`STT_ENABLED`/`STT_TYPE`/`STT_MICROPHONE` 是个人配置。读任何配置前先查它登记在 `ServerRuleConfig` 还是 `GeneralConfig`。同理记住：**LLM/TTS 站点每只女仆各选，STT 是全局单选**（服务端 `ServerConfig.SERVER_STT_TYPE`、客户端 `AIConfig.STT_TYPE`）。**同一个字段在不同服务上未必都是判定点**：`enabled` 在 LLM/TTS 各有三处真实判定，在 STT 链路上一处都没有——据它报「站点未启用」就是纯误报，我当天犯了并在一小时内自纠 |
| 用 python 脚本批量改文件时，`s.replace(old, new)` 没匹配上顶多是「没改到」，看输出就知道 | 2026-07-27 一天内静默失败两次：改 `tree_equiv.py` 的判定逻辑（以为改了，实际原文没动）、改 `AiReloadWiringContractTest.activeSource`（同样静默 no-op，直到测试用错误的理由失败才发现） | **`replace` 必须带 `assert s.count(old)==1`**，否则失败是无声的，而下一步的验证会把「没改到」误读成「改了但没生效」，把人引向完全错误的排查方向。同理：**改完必须回读那一段**，不能只看脚本有没有报错 |
| 一条「对玩家不可见」的旁路，只要不主动画 UI 就真的不可见（新加的工具执行旁路用 `subagents=true` 不建等待气泡、`onSuccess` 空实现，认定它不碰界面） | 用户截图：女仆已经回话了，头顶却一直挂着「少女思考中……切换为 坐下」。父类在执行工具时会调 `refreshWaitingChatBubble`，那是主对话的进度提示——它**新建**一个气泡，而空的 `onSuccess` 没有任何人来收尾，只能挂到 90 秒超时 | **不可见性是要显式实现的，不是不写代码就自动获得的。** 复用一条为「可见流程」写的基类时，**它继承来的每个副作用都得逐个决定要不要**——尤其是那些由**别人**（工具）在中途触发的。判据：这条路径上还有谁会调基类的方法？另：`refreshThinkingText` 对 `id >= 0` 先 remove，而 **0 是合法 id**——用 0 当「没有」的哨兵，会误删别人的东西 |
| 诊断用的命令是只读的，随时可以跑（rig 正在启动服务端时，顺手跑 `gradlew compileJava` 查代码有没有编译错） | 服务端起来后立刻崩：`ClassNotFoundException: datagen.tag.TagBlock`。查时间戳——那个 `.class` 的写入时间比崩溃**晚 11 秒**：两个 Gradle 构建同时写同一个 `build/classes`，服务端读到了正在被重写的半成品目录 | **`gradlew <任何任务>` 都不是只读的**，它会重写 `build/`。rig 启动期间禁止再跑任何 gradle。更一般地：**排查工具与被测系统共用可变状态时，排查本身就是一次干扰**——差点把自家工具的并发写成产品缺陷去修 |
| 等待循环报 READY 就是真的 READY（`cycle.ps1` 报 `SERVER=READY / CLIENT=JOINED`） | 实际上**没有任何服务端进程**、25565 不在听、`latest.log` 停在上一轮。它读到的是**上一轮遗留的日志** | 「不要读上一轮的判决」若靠「MC 启动时会截断 latest.log」实现，就有一个缺口：**服务端从未启动时什么都不会被截断**。判据必须是自己能控制的——**启动前先删掉日志**，让「从未启动」表现为「没有进展」而不是「成功」。与「让『没进展』本身成为可检测信号」同族，但这次栽在「成功信号是陈旧的」这一侧 |
| 同一个症状家族就是同一个根因（「长对话后不调工具」与「TTS 说错语言」都表现为「上下文里只有反例」，故断定同属位置竞争失败，拟按同族做无历史意图判定 + `tool_choice=required` 重问） | A/B 只换模型档位：`deepseek-v4-flash` **0/2**、`deepseek-v4-pro` **2/2**（历史更长更不利仍成功）——强档位在同样的反例历史下照样调用 | **「机制看着像」不等于「机制相同」。** 动架构前必须先排除**能力/档位**这个混淆变量——它最便宜、最容易被跳过，而跳过它就会为一个不存在的架构缺陷写代码。判据：**同条件只换模型**。这条与「一次只动一个变量」同源，但更隐蔽——因为前一个同族缺陷刚被证实过，惯性会让人直接套用结论 |
| 症状发生在哪个子系统，故障就在哪个子系统（「云端 STT + 网络不畅 → 客户端冻结」→ 三轮排查 STT 的请求链路，四个客户端全用 `sendAsync`，读不出阻塞点） | 抓栈实测：渲染线程卡在 `displayClientMessage → ChatListener → PlayerSocialManager.isBlocked → forceFetchBlockList`——**原版在渲染线程上同步查 Mojang 屏蔽名单**，与 STT 的网络毫无关系，只是恰好并发 | **「A 时候发生」不等于「A 造成」。** 冻结类问题不要读代码猜，**直接抓栈**：连续采样 + 看 CPU 时间是否不变（不变＝真阻塞而非忙碌）。另：我们的消息格式 `<玩家名> 文本` 正好命中 `guessChatUUID` 的 `<...>` 解析——**照抄聊天外观的文案会继承聊天的全部处理链** |
| 仪器没输出 = 被测对象没问题 | 按命令行含 `runClientDedicated` 过滤进程，采到的是 **gradle 包装器**（它命令行里也有这个词），5 分钟零样本读起来与「渲染线程从未阻塞」完全一样 | **相信仪器的沉默之前，先证明仪器是活的。** 判据写进脚本：启动时探一次目标必须有 `Render thread`，没有就 `ABORT` 退出。选进程的判据是 `dli.env=client`，不是任何会出现在包装器命令行里的词。**另**：判据本身也会太宽——第一版把「非空闲」当阻塞，而渲染线程 `RUNNABLE` 做渲染活也非空闲，改成「park/阻塞态 或 socket/音频/HTTP 帧」才照出真阻塞 |
| 契约测试用 `indexOf` 在**整份文件**里找调用点就够了 | 把一个 reload 方法拆成三个之后，`AvailableSites.init()` 与 `SkillLoader.init()` 各有多处，`indexOf` 找到的是恰好排在前面那个方法里的，先后关系断言随之失去意义（三条用例挂了两条） | 断言方法内部的**顺序/相邻关系**时，必须先把范围缩到那个方法体（大括号配对截取），否则测试的有效性依赖「文件里只有一处」这个随时会变的前提。**另**：剥注释只剥 `//` 不剥 javadoc，会让「只在注释里存在的接线」被判为存在 |
| 「用户实测永远胜出」适用于用户说的**每一句话**，所以用户断言「没有对称问题，客户端那三个文件是给单人档用的，进服务器后就用服务器的」时直接采信并写进文档 | 用户当场指出「我说的话也不是绝对的，你也要去验证」；逐点查证后：那句**只对 `llm.json` 成立**，`tts.json`（系统 TTS 在客户端播放）与 `stt.json`（识别全在客户端）**进服务器后照样在用** | **纪律 1 只覆盖「实机可观察行为」**——那种场合我推不过用户。但**代码结构的断言我查得了，就必须查**。本例更值得警惕的是：**结论对、理由错**（正确判据是「该文件在它所在那一端有没有任何模式下的消费者」）。照错理由推下去会删掉客户端 `tts.json`，系统 TTS 直接哑掉。**下一个人复用的是理由，不是结论。** |
| 「网络序列化」与「文件序列化」天然是两套，所以改网络形态不动存档；顺带以为自己列的通道表是全的 | `SerializableSite.writeToNetwork` 的默认实现**直接用 `codec()`**，全仓零覆写——同一套。按 `writeToNetwork` 反查调用点，还揪出通道表漏掉的 `SyncServerRulesPacket`（每次玩家加入就发含密钥的站点） | **用途不产生实现**：要断言「有两套」，必须找到两个实现，不能按用途推。**枚举「谁在传 X」只能按 X 的写入函数 grep**，不能按包名、目录或记忆列——我列的六条通道漏了一条，而那条是每次加入都发的 |
| 常量名叫 `MAX_PAYLOAD_SIZE` 就是该类包的上限（据此以为 C2S payload 卡在 32767，要自己写分片） | 读它的消费点：`ServerboundCustomPayloadPacket` 只把 32767 交给 `DiscardedPayload.codec(id, 32767)`，即**未注册 id 的兜底解码器**；注册过的 payload 走自己的 codec 从不经过它 | **常量的消费点才定义它约束谁**，名字不定义。与「javap 确认方法存在不够，还要确认参数语义」同族：读到一个上限值，必须回答「谁在读它、读它的那条分支我会不会走」。真上限在别处——`Varint21LengthFieldPrepender` 的 3 字节 varint 帧长 2,097,151 B |
| 「仓库里已有 `OpusAudioStream`，反过来做是对称的」——有解码就等于有编码 | `OpusAudioStream` / `OpusDecoderUtil` 全在**解码**侧（放 TTS 音频用），录音链路是裸 PCM 封 WAV，一句话 96~160 KB 而非文档写的「几十 KB」 | **方向性能力不能按名字推对称。** 编解码 / 上下行 / 读写是成对概念但不是成对实现，仓库有一半从不蕴含有另一半。凡文档写「反过来做是对称的」，先去数另一半的代码在哪 |
| Write 工具产出的 `.ps1` 能直接被 PowerShell 跑 | 中文注释里的 `。` 被按 ANSI 解码，报「字符串缺少终止符」等一堆假语法错 | Write 产出**无 BOM UTF-8**，而 Windows PowerShell 5.1 对无 BOM 文件按 ANSI 解码。含非 ASCII 的 `.ps1` **必须存成 UTF-8 with BOM**（本仓库既有 `release-gate.ps1` 就是 `EF BB BF` 开头） |
| 目标平台有现成机制、且用途看起来对得上，就该用它（Forge capability → Fabric 就该映射到 cardinal-components，因为 fork 已有五个 component 的现成模式） | 读基准才发现 `MaidCapability` 与 Provider 都是 `@OnlyIn(CLIENT)` 且实现 `ICapabilityProvider` 而**非** `ICapabilitySerializable`——零持久化零同步的纯客户端缓存；挂成 component 会凭空添上存档与同步语义 | **迁移映射要按「被迁移物的语义」定，不是按「源平台的类名」或「目标平台有什么」定。** 判据是三问：它持久化吗？它同步吗？它的生命周期挂在谁身上？——三问都答完再选机制。同一源平台机制（capability）会对应目标平台多种机制，选错的方向通常是「选了更重的那个」 |
| 「只关自己 `Start-Process` 返回的 pid」这条纪律执行了，就不会误伤也不会留残 | 2026-07-28 收工后用户发现专服还在跑：`Start-Process gradlew.bat` 返回的是 **gradle 包装器**的 pid，真正的游戏 JVM 是其子进程，杀包装器时 JVM 已脱离、继续运行 | **守住流程不等于操作对了对象。** 判据要从「我记下的 pid」升级成「能证明是我的进程」：查命令行里的 `argFiles/runServer|runClient*`、`--gameDir`、占用的端口，三者任一能钉住归属；反过来，**不能证明是我的就不动**（本次 `15576` 无线索即放过，`48788` 的 gameDir 指向真实启动器实例即放过）。与「javap 确认方法存在不够、还要确认参数语义」同族 |
| 等待循环把「成功关键字 + 我想到的失败关键字」列全就够了 | 2026-07-28 同一天栽两次：客户端崩溃形态不在过滤里（当作还在跑）；`.cmd` 报「不是内部或外部命令」（中文、且在 stderr）不在模式里，循环**空转十分钟**，用户来问才发现 | **别指望把失败措辞列全——让「没进展」本身成为可检测信号。** 等待循环必须带兜底判据：日志字节数连续 N 次不变即 STALLED 退出，或直接查进程是否已退出。判据从「命中了什么」改成「有没有在动」，才对未知失败形态免疫 |
| 启动客户端前「清掉残留的客户端进程」是安全的清场动作 | 2026-07-28 我用 `Stop-Process` 无条件杀掉所有 `KnotClient`，把**并行会话正在做的测试**一起杀了；用户当场制止 | **共享机器上没有「我的进程」这回事。** 与「共享工作树会扫走别人未提交改动」同族：判据不是「这类进程该不该留」，而是**「这个具体进程是不是我起的」**。做法：记下自己 `Start-Process` 返回的 pid，只对它 `Stop-Process`；发现有不认识的客户端在跑就**换 runDir**（`clientDedicated` 就是为此存在）或直接报告并等待，绝不代人清场 |
| grep 没搜到「谁调用 addLayer」，就写下注释断言「该 layer 列表恒为空」 | 实测崩在 `GeckoLayerMaidBackpack:24`——TLM 的 `GeckoEntityMaidRenderer` **构造函数里**就 addLayer 了 5 个 | **没搜到 ≠ 不存在。** 我搜的是「谁调用」，而它是在构造函数里自己调的，不在我的搜索面内。写「恒为空 / 无人使用 / 零消费者」这类**全称否定断言前**，必须把构造函数、静态初始化块、事件注册三处也扫一遍，否则就只写「我搜过 X 模式没命中」而不要升级成事实 |
| 一类问题扫一遍就干净了——把 `EntityMaid` 上的 vanilla 继承成员全改经 vanilla 静态类型后，dev 映射假象就修完了 | 同一根因立刻在 `EntityMaidRenderState` 上再炸一次（`method_62613` = `hasPose`） | **扫描的判据要按「问题的成因维度」定，不是按我脑中的对象类别定。** 成因是「owner 是别的 mod 的类 + 成员继承自 vanilla」，所以判据是**凡来自那个 mod 的类型**（实体、render state、data 载体……），我却按「实体」这一类扫。修法也要选结构性的：把 helper 形参直接改成 vanilla 类型，让 owner 天然正确，而不是逐个调用点加转型靠自觉 |
| 并行会话各自的未提交改动互不干扰——工作树刚查过是干净的，我的改动在我提交前都归我；对方已落库的提交 SHA 可以放心写进账本 | KubeJS 解耦 5 个文件改完未提交，被并行会话扫入其 GUI 提交（空 patch 报错才暴露）；我把该 SHA 写进三处账本后**对方又改写顶端拆分提交**，`283b00052` 几分钟内悬空 | **共享工作树上，任何会话的 commit 都可能扫走别人的未提交改动；对方未推送的顶端提交还随时会被改写。** 并行期间：① 改动成形立即定点提交，只 stage 自己的 pathspec；② 每次 diff/提交前重查 HEAD 与 status（开局快照会过时）；③ 门禁在隔离 worktree 跑，不在主树跑；④ 引用对方提交 SHA 前确认已稳定（或以内容描述记账），写完账本复核可达性 |
| 门禁报某一关失败时，紧邻输出里最显眼的那个异常就是原因（`facts.py` 打出「常驻文档超预算 ★」，同屏「文档 lint 失败」→ 断定是预算撑破了，还据此让用户批准调高预算） | 读 `doc_lint.py` 才发现它**根本不检查行数预算**；那段快照的抬头就写着「不参与判定」。真实原因是账本里引用了 fork 仓库的 SHA | **失败归因必须读那个判定工具本身，不能从相邻的显眼输出推断。** 一个工具打印的警告与另一个工具的失败同屏出现，只是排版相邻。这与「症状文案指向哪里故障未必在哪里」同族，但更隐蔽——**这次误导我的不是产品文案，是我们自己写的诊断工具**。凡「X 失败」，先定位判定 X 的那段代码 |
| 我在提交信息和代码注释里写下的契约，就是代码的实际行为 | 我在 hub 的字段注释、javadoc、提交信息**三处**写着「在任何一页点保存都会把攒着的全部改动一起提交」，而语音输入页的保存**从不碰共享暂存**，还无条件闪「已保存」——两次点击就能让世界规则静默丢失且用户被告知成功 | **自己写的契约是意图，不是证据。** 越是言之凿凿写了三遍，越不会有人回去核对。**凡写下「所有 X 都会 Y」这类全称契约，当场加一条按 X 枚举的测试**（本例：枚举有保存按钮的屏，断言各自提交共享暂存），否则契约只是一句愿望，而下一个读它的人（包括我自己）会当成事实 |
| 文档里的提交号只要真实存在就没问题 | 照抄并行会话的收工摘要时把 fork 仓库的 SHA 写进账本，`doc_lint` 当场判为不存在的提交——而对方上一笔提交刚好就是在清理同类问题 | **账本只写本仓库可达的 SHA。** 外部仓库一律用「仓库名 + 分支」描述。更一般地：**从别人的总结里搬运内容时，搬的是内容也搬了它的上下文假设**——那份摘要写给 fork 的语境，进本账本就失真 |
| 单人档实机全项通过 = 这批改动验过了 | 遥测与日志双重佐证那次验收是 `server_type: local`；而我当天把试听的站点解析改成读**客户端**的 `AvailableSites`，专服上两侧各读自己那份 `sites/tts.json`——「服务端有、客户端没有」的站点会被客户端拒发，**单人档里两张表是同一张，按定义测不出来** | **验收通过要连同它的环境一起记账。** 单人档是「客户端与服务端共用同一份数据」的特例，凡我改动触及「两侧各有一份」的东西（站点表、配置文件、权限），单人档全绿都不构成证据。改这类代码时先问：**这两侧的数据在什么环境下会不一样？** |
| 「HTTP MCP 只在会话启动那一刻连一次，连不上不重试也不报错」——据此以为可以写个阻塞式 `SessionStart` hook，先把游戏拉起来再让它连 | 探针实测（2026-07-29，TCP 监听 + sleep 12s 的 hook + 无头 `claude -p`）：`CONNECT` 在进程启动后 **1.3 秒**发生，比 `HOOK-START` 还早 0.46 秒；1 次首连 + 3 次重试（退避约 0.5/1.5/4 秒）在 hook 还睡着时就全部用完，hook 睡满 12 秒结束后**再无任何连接** | **连接窗口约 6 秒，且先于 hook**——任何在会话内部做的补救都够不着，服务必须比 Claude Code 进程先起（`docs/tools/mcp-up.ps1`）。原描述另有一半过期：**确实有 3 次重试，失败也会经 ToolSearch 报给我**，不再是全静默。`MCP_TIMEOUT` 无用（连接被拒是立即失败，走不到超时）；`alwaysLoad` 会阻塞启动但上限仅 5 秒。补救靠 `/mcp` 面板手动重连，不必重开会话 |
| 崩溃报告的标题就是第一现场 | 报告头条是 `GameRenderer.extractCamera` 的 NPE；真正的第一现场在它上一行——登录包处理失败导致玩家实体没建出来，渲染线程照常渲染世界，自然拿不到相机实体 | **先问「这个异常是因还是果」。** 崩溃报告写的是**进程死在哪**，不是**哪里先坏**。凡看到 NPE/AIOOBE 这类"下游症状型"异常，先在同一份日志里往上找有没有更早的 ERROR——尤其是带 `Failed to handle packet` / `disconnecting` 字样的 |
| 异常栈顶那个类就是罪魁 | `DecoderException` 栈顶是 TLM 的 `AltarRecipeSerializer.fromNetwork` 读出 `Index 482 out of bounds for length 4`，看着像我们的序列化器写读不对称；实测把服务端 2722 个配方逐个 encode→decode **全部对称**，真因是家具重制按**数字注册 id** 分派序列化器而两端 id 不一致 | **流式解码里，栈顶只说明"错位之后恰好轮到谁"，不说明谁造成错位。** 凡 `DecoderException` / 缓冲越界，先问「这条流是从哪一项开始错位的」，再问「谁写的这条流」。判据是**把可疑单元逐个独立往返**——全都对称就说明问题不在单元里，在框架/顺序上 |
| 出问题时同时换两个变量能省一轮 | 为了赶进度一次同时装上 framework+refurbished 又摘掉 YSM，结果拿到一个全新故障、归不了因，反而多花一轮才把 2×2 补齐 | **一次只动一个变量，没有例外。** 赶时间时最想违反这条，而违反它的代价恰恰是更多轮次。四格 2×2 补齐后归因只用了一次运行 |
| 我照抄基准的常量，就是照抄了基准的行为 | 把 `0xff8800` / `0xff0000` 原样搬来，1.21.1 的 `Font` 会把 alpha=0 补成不透明、1.21.11 删了这个兜底，于是标记**全透明**——事件在跑、组件读到、几何算对、绘制执行、日志干净，屏幕上什么都没有 | **常量携带着它对消费方 API 的假设。** 照抄一个字面值等于照抄「消费它的那个 API 会怎么解释它」。跨版本移植时，凡颜色/位掩码/枚举序号这类**被别人解释**的常量，必须回去读消费点在新版本怎么解释 |
| 「装上修复后能进服了」就说明修复有效 | 把修复去掉做 A/B，**结果照样进服**——触发故障的那个包（家具重制的工作台配方同步）不是每次都发。若据首次绿灯收工，会把「无效」当「有效」，或反过来 | **症状是概率性的，机制不是。** 凡协议 / 时序 / 竞态类结论，判据要落在**机制的可观测量**上（本例：两端注册表 id 逐项比对，16/38 → 0/38），不能落在「这次跑通了」。一次绿灯只在故障是确定性的时候才算证据 |
| 用 `python -c` 传含反引号的 Markdown 去改文档是安全的 | bash 把反引号当命令替换，`RecipeHolder.STREAM_CODEC` 等整串被吃掉，写进文档的是**残缺句子**，还顺带报了一串「command not found」 | 与「Bash heredoc 吃反斜杠」同族：**含反引号 / 反斜杠 / `$` 的内容一律用 Write 或 Edit 工具写**，不要过 shell。改完必须回读那一段——本例是靠回读发现的 |
| 等待循环读到日志里有失败字样，那就是这一轮的失败 | `cycle.ps1` 匹配 `debug.log`，而 MC 启动时旧内容还在，于是把**上一轮**的 `Client disconnected` 读成了本轮结果，报了个假 DISCONNECTED | **跨轮次复用的日志文件必须先确认它属于本轮**（比较起始时间戳或先删除），否则等待循环会把历史当现状。与「探针记 result 非 null 就等于有提交」同族：**判据要能区分"这一轮的"与"还留着的"** |
| 核对第三方审计报告 = 验证「它说的现象在不在」 | 上游审计说 #912「棋盘被破坏后女仆卡在棋局状态」；本地读码发现 `MaidGameRecordManager.tick()` 反而**每 tick 都清**状态——移植期丢了 `!(vehicle instanceof EntitySit)` 判据，胜负动画在本 fork 里**从来没播放过**，而这个判据同时正是基准自愈 #912 的机制 | **审计是按上游源码写的，本地在同一处可能是不同甚至相反的缺陷。** 核对的问法必须是「这段代码在本地长什么样、在 `origin/1.21.1` 长什么样」，而不是「审计说的症状在不在」。三方对照缺一不可——只比本地与审计，会把一个反向的移植回归整个漏掉；本轮 7 条里 6 条与基准逐字相同（真上游缺陷），唯独这条不是 |
| 门禁绿 / `facts.py` 报「GameTest 65 项，失败 0」= 我新加的测试通过了 | 那行明写「上次 runGametest 的结果」，而项数是**磁盘上的 @GameTest 方法数**（58+7）——新测试当时一次都没跑过；`release-gate.ps1` 也不跑 runGametest | **数字的来源比数字本身重要**：同一屏里「项数」来自静态扫描、「失败数」来自上次运行，拼在一起读就成了假证据。与「相邻输出不等于因果」同族。另：**测试跑绿 ≠ 测试有效**——必须把缺陷逐条装回去做红测反证（本轮 4 条针对性用例全部验过，3 条对照路径恒绿属预期） |
| 端点握手通了、世界里有 `touhou_little_maid:maid`，那就是本仓库的构建在跑 | 8765/8766 的 owner 是 PID 60048，命令行指向 `npct\MaidRestaurant` 的 `runClient`——**另一个项目的开发客户端**，女仆来自它依赖的那份 TLM，与本分支无关 | **「里面有我要的东西」不等于「这是我的实例」**，判据只能是进程命令行里的项目路径（`argFiles` / `gameDir`），与「不能证明是我的进程就不动」同族。同轮还实测到 8765 **已在 LISTEN 但 HTTP 打不通**（世界加载中，先绑端口后注册 handler），故就绪判据必须是 initialize 握手；反过来**「握手不通」也不等于「没人在用」**，共享机器上启动第二个实例前必须先查端口占用，否则会抢掉别人正在跑的测试 |
| 门禁脚本存在、被调用、且打绿灯，那道检查就在跑 | `release-gate.ps1 -Release` 把 `--release` 当**唯一参数**传给 `tree_equiv.py`，而后者按位置取内部 ref → `git diff` 拿到坏 ref 失败 → `git()` 是 `check=False` 只取 stdout → 返回空 → 空被读成「零差异」→ 绿灯。**这道闸从上线起就没真正跑过**，v0.8.4 即在此假绿下发布；修复后同一比较报出 114 处真实差异 | **「机械化的规则」本身也需要被验证**——tree_equiv 的文件头恰好写着「靠纪律守不住，规则必须机械化」。三条通用判据：① 吞掉子进程错误 + 空结果读成通过 = 假绿工厂，**空输出与命令失败必须可区分**；② 位置参数与开关混传，坏参数会伪装成合法输入；③ 新加一道闸时必须**红测**——喂一个该失败的输入，确认它真的红 |
| 我选了个能隔离变量的测量点，就能观测到目标现象（探针记「骨骼链自身贡献」= before⁻¹·after，据此判「挂件随骨骼动」） | 四种姿态下该量恒为 `(0,1.875,0)`+单位旋转，我判不了是「刚性绑定本该不变」还是「链选错了」；用户实机一看**泳姿正常**，判据当场成立 | **隔离变量的测量点，往往正好把目标现象隔离在外**。挂点刚性绑在身体上，身体的变换在测量点**上游**——我测的那个量按构造就不可能变。选测点前先问：**如果结论为真，我这个量会怎么变？答不出来就是选错了点** |
| 探针放在被测代码「附近」就行 | 第一版把采样点放在 `visitor.accept` **之后**，背包层自己的 `translate(0,1,0.25)+ZP 180°` 混进了测量值（`m00=-1,m11=-1` 正是那个旋转） | 探针的位置是**测量定义的一部分**，不是实现细节。放之前放之后测的是两个不同的量 |
| 「fork 侧不需要任何改动、骨骼桥已就绪」（交接提示词的前提） | fork 的 `renderTlmLayers` 是**空方法体**，不填它 TLM 侧再完美也零消费者 | **交接文档写的是上一个人的认识，不是事实。** 凡文档断言「另一侧已就绪」，动手前去读那一侧的**方法体**，不是读它的注释——那段注释还言之凿凿解释了「为什么不调用」 |
| 签名里没有这个参数，就说明这条路上拿不到它（`geoRender` 无 `CameraRenderState` → 设计 bridge 传 null，还加了条禁用 camera 的断言） | 读 fork 参数链时撞见 `state.camera`：`EntityMaidRenderState` 本来就有，`EntityMaidRenderer:130` 在移交前填好，fork 自己也在用 | **签名里没有 ≠ 拿不到。** 值可能挂在已经传进来的对象上。加约束前先把手上对象的字段翻一遍，否则会为不存在的约束写测试，反过来挡住将来正当的用法 |
| 让模型在回复正文里用 `---` 分出第二段是可行通道，多轮后顶多偶尔漂移，把要求重述在历史之后即可压住 | 逐轮读请求与响应原文：第 1 轮给第二段，**第 2 轮起再也不给**——历史里只要出现 1 条单段助手回复就翻转，之后恒不给。不是概率漂移，是确定性单调翻转 | **把结构化字段塞进自由文本通道，等于把协议合规交给「示范 vs 指令」的竞争，而示范总赢。** 判据是问：**这个字段有没有一条正例出现在上下文里？** 只有反例时，指令必败，位置竞争也救不了。修法只能是把它移出该通道（无历史的独立请求 / 结构化字段），不是把指令写得更重。另：**别按用途推实现**——「同一条回复里带两段」看着省一次请求，实测代价是功能根本不工作 |
| 症状相同就是同一个缺陷，修好一个即两个都好（O3 把「不调工具」与「TTS 说错语言」按「同根」一起记成关闭） | `893831008` 确实修掉了「照抄旧语言」这个机制，但 TTS 说错语言**原样保留**——它换成了「不给第二段 + 兜底静默」这条新机制 | **症状相同不等于机制相同。** 一个修复可能换掉机制而保留症状。关闭一条含多个症状的条目时，**必须分别写明每个症状是怎么验的**；只写「同根，已修」就没人回去验第二半，而账本从此说谎 |
| 目视判读能分辨「稍微陷进去」与「正好」，据此二分调参是可行的 | YSM 坐姿偏移：三轮「三条都稍微陷进去」的口头判读，导出一个统一 trim（试过 0/0.05/0.1）；改用 F3+B 碰撞箱实测后发现**那批判读全落在 ±0.03 格的噪声带里**，四个常量里只有一个真需要改 | **目视有噪声带，先量出它再谈调参。** 量法要找一个与被测物**同深度平面**的已知尺度当标尺（此处是碰撞箱宽 0.6 格），透视对两者一视同仁，近景也准。噪声带内的「感觉偏高/偏低」不是信号，照它二分只会横跳。与「性能类结论至少测两次」同族：**单次主观采样区分不了信号与噪声** |
| 发布页要如实标注仍开放的验收项（我据 §9.4「已知限制」写了整段验收覆盖率） | 用户直接删掉整段，连我特意保留的第三方模组缺陷两条一起 | §9.4 写的是**「已知限制」**，我交的是**「我们的测试进度」**——读到「尚未逐项目视验收」的人既不能规避也不能据以判断，只折损信任。**如实记账的义务在内部账本，不在发布页。** 我还自造了「GitHub Release 是给技术读者看的」来开例外，可到那页的人是去下载 JAR 的，与 Modrinth 同一批人。同轮我在更新日志上犯过一次同形错误（写根因），**被退回后只改了一处，另一处用规范条文给自己背书**——退回一次就要问「同一个毛病还在哪儿」 |
| `git check-ignore -v` 打出了匹配行，就说明该路径被忽略 | 它匹配到的是 `.gitignore` **末尾的空行**（我查询时多带了尾斜杠），而 `git status` 一直把该目录报成 `??` | **两个工具给出矛盾信号时，先分清谁是权威。** `status` 才直接回答「git 现在怎么看这个路径」，`check-ignore` 回答的是「哪条规则匹配」，后者在退化输入下会给出无意义的匹配。判据选那个**直接回答我的问题**的命令 |
| 兼容补丁只要逻辑原样搬运就安全（1.21.1 钓线的 Oculus 补丁顶点原样搬进 1.21.11 重写） | 女仆钓线 Iris 在场即满屏乱线：旧补丁是给 **17 顶点 lineStrip 补成偶数**用的，新写法本身已是偶数 32 的成对 lines()，+1 反而破坏偶数，且 SubmitNodeCollector 同 RenderType 合批把错位放大到同批全部线段 | **兼容补丁携带着它对宿主代码形状的假设**。宿主重写后补丁的前提可能整个反转——搬运前先回答「它当初在补什么条件？该条件现在还成立吗？」。同族：TrackerMarkerOverlay 的 alpha/置顶教训 |
| 延后 TODO 里写的障碍类名是真实存在的（「restore when ItemGohei is un-excluded」） | 全仓 TODO 审计：`ItemGohei` **从未存在过**（真名 `ItemHakureiGohei`，基准与本树皆然、一直在编译），御币动画分类整个白丢一个版本；同轮同型还有「Patchouli excluded」（早已 compile-only 在库）与气泡「行为不受影响」（丢了玩家可见提示） | **延后注释的「障碍」也要核对其存在性**，它可能从写下那刻就是错的。凡「restore when X」先查 X 是不是真障碍、以及理由是否早已消除——本轮 4 处全是理由消失无人回收（R7 型），审计入口就是 grep 延后字样 |
| 「有 26.x 版本」＝「我们能用」（据 Modrinth 搜索结果里的 `versions` 含 26.1.2，判定 KubeJS / Aquaculture / Sophisticated Backpacks 在 26.1.2 上重新可用，据此准备把放弃的兼容拿回来） | 按 `loaders` 字段逐条复查：三者的 26.x **全是 NeoForge**；Accessories 连一个 26.x 都没有，连带三项背包兼容仍然不可能。整批 19 项里 Fabric 侧可用的是 0 | **依赖可用性必须查到加载器粒度。** 搜索接口返回的 `game_versions` 是**跨加载器并集**，拿它判 Fabric 可用性会得出相反结论。同族的反向错误在同一轮也犯过：**基准仓库 `build.gradle` 里注释掉的依赖行不等于「该模组没有新版」**——Carry On / PatPat 都有 26.x 正式版，只是基准还没接回去。判据恒为**模组自己的发布页 + 该构件的 loaders 字段**，不取任何第三方的依赖清单 |
| 按 conventional 前缀（`feat` / `fix` / `perf`…）分类提交，就能把「差异化」与「移植苦力活」分干净 | 本分支有 **39 笔提交没有前缀**（`Add typed Kaleidoscope Tavern compatibility`、`Complete semantic audit fixes and altar recipe sync`…），被分类器**整批静默丢弃**；「祭坛配方客户端同步」——我们独有、新基根本没有那两个文件——因此漏报，靠「文件面 × 文档面 × 公开面」三面交叉才捞回来 | **枚举的判据不能建立在「大家都遵守某个书写约定」上。** 凡要求完整性的清单，至少要两条独立通道交叉：一条**机械可重算**的（那个文件在对照树里存不存在）+ 一条**声明性**的（账本的「我们独有」、公开 README 的承诺）。单通道只能给**下界**，且必须在文档里写明它是下界而非全集——否则下一个人会把它当全集用 |
| 派子代理做机械扫描，它给出的分类与「影响」结论可以直接用 | 四个 haiku 代理各交回一张表，逐条复核后：删除类数目从它报的 278 改成 262（它的改名过滤漏了本仓库的 `TileEntity→BlockEntity` 约定）；它给每条都写了「Impact: 整个 XX 系统缺失」，而抽样发现相当一部分是**架构替换**（JS 动画 → molang 绑定）、**内部重构**（geckolib3 删 25 个类但该包仍有 218 个文件）或**库合并** | **子代理适合产出候选与证据，不适合下判定。** 它不掌握本仓库的改名约定、生态裁决（§2.2 哪些模组在目标版本上根本不存在）、以及「删掉 = 换了实现」这类上下文，于是会把重构报成缺失。用法：让它交**原始命中 + 证据 + 一句话说明**，明确禁止它写「影响」栏；判定与计数一律主模型重算。与证伪表里「子代理的审计结论可直接采信」那条同源，这次是在**统计口径**上重演 |
| 移植的活儿就是「把我们多出来的那部分搬到新基上」，新基原本就有的东西不用管 | 新加的 mixin 登记闸门首跑照出 `client.HumanoidModelMixin`：`origin/1.21.1` 上就有的功能（扛女仆时玩家手臂摆抱姿），被宿主在迁移期**注释掉 + `@Mixin` 靶点改指 `Dummy` 空壳 + 留 `FIXME` + 不登记**，于是相对行为基准是一处回归，而它**不在任何差异化清单里**——因为它本来就不是差异化 | **「宿主完成了版本迁移」不等于「宿主迁移得完整」。** 差异化清单只回答「我们多出来的部分到齐了没」，回答不了「宿主自己丢了什么」。后者要靠**独立于清单的机械闸门**去照（本例是 mixin 登记扫描），或按「基准有 → 宿主有没有」正查一遍。同族手法在本宿主至少两处（`compat.improvedmobs.EventHandlerMixin` 同款，只是它登记了所以闸门抓不到），**说明这是宿主的习惯性搁置手法，不是孤例** |
| 给「禁止某种写法」的契约测试配一条「命中数 ≥ N」的下限，就防住了零覆盖 | 读点契约拆成两层后，第一层（禁止直接 `X.get()`）的**正确命中数就是 0**，那条下限当场把绿测判红——我把「有多少违规」当成了「扫描还活着」的证据 | **活性判据必须与被断言的量正交。** 「没有违规」与「扫描根本没跑」在结果上完全一样，所以活性要用**独立于结论**的量来证：走过了多少个源文件、加载了多少个看管对象。用「命中数」当活性证明，只在「预期命中数 > 0」的那类断言里成立；禁止型断言恰恰相反 |
| 「读点」就是写了 `X.get()` 的地方，按这个模式扫一遍就扫干净了 | 契约测试红测实证：`targetConditionsTest(maid, target, MaidConfig.BOW_RANGE)` 把**配置对象当参数传进去**、在方法体里 `configRange.get()`——读点藏在形参后面，四条攻击任务全漏改；另有两处 `import static ...MiscConfig.MAID_FAIRY_BLACKLIST_DIMENSION` 后写**裸名字**，同样扫不到 | **判据要按「成因维度」定，不是按「症状的书写形态」定。** 成因是「拿到了那个对象的引用」，不是「写了 `.get()`」，所以判据应为「除了递给唯一读口，不许对它做任何别的事」。修法也要选结构性的：把 helper 形参从配置对象改成**已解析的值**，让调用点天然正确，而不是逐个调用点靠自觉。同族：「扫描的判据要按问题的成因维度定」那条，这次栽在「间接引用」与「静态导入别名」两种绕过形态上 |
| 门禁脚本接管道再 `&&` 串下一步，跑绿就是绿 | `doc_lint.py \| tail -5 && git commit`：管道退出码取自 `tail` 恒为 0，闸口报红也照样提交，我当场把一个 lint 失败提交了进去 | **门禁命令不许接管道再串 `&&`**。要么单独跑读 `$?`，要么用 `PIPESTATUS`。与证伪表里「吞掉子进程错误 + 空结果读成通过 = 假绿工厂」同源，只是这次假绿出在**调用方式**上而不是脚本内部——工具本身是好的，是我把它的判决丢了 |
| 把整份方法论复制到新分支，改掉版本号就算完成迁移（本文件自 1.21.11 原样搬入，只换了分支名与版本表） | 2026-08-13 逐条查证：核心纪律**第 2 条方向是反的**（它禁止「为贴近 26.1 改行为」，而本分支 `origin/26.1` 是代码宿主，写法本就该照它）；第 3 条的「排除 = 隐形」前提**不存在**（新基零条源码排除）；第 8 条的命名约定**是假的**；第 4 条无适用对象；`docs/COMPAT.md` 这个指路**没跟过来** | **复制方法论，继承的不只是经验，还有它的前提。** 每条纪律都是在某个语境里成立的，换语境后**经验仍有价值、前提未必成立**，而前提失效的那条会把人主动引向错误方向（第 2 条尤甚：照它做就等于拒绝新基的正确写法）。判据：逐条问「**这条依赖的前提在新语境还成立吗**」，而不是只改显眼的版本号与路径。改法是**逐条改并注明「原文为 X 语境，本分支据 Y 修正」**——下一个人复用的是理由，不是结论；**别整份重写**，那会把继承来的经验一起冲掉 |
| 提交信息写了「上一刀留的锚点由本刀闭合」，锚点就闭合了（`be630452f` 如此声称，还把 API 漂移、红测、依赖实查都写得很扎实） | 审计数调用者：`BoardStateTooltip.ofGomoku` 三个工厂**全仓零调用者**——那刀只加了组件/渲染器/映射三个文件，**从未碰 ItemBoardState**，锚点 javadoc 原样躺着，整条预览链是死代码 | **「闭合锚点」类声明有机械判据：锚点所在文件必须出现在那笔提交的 diff 文件列表里**，不在就是没闭合，不用读代码就能审。与「自己写的契约是意图不是证据」同族，但更隐蔽——提交信息其余部分越翔实，这一句谎越难被怀疑。链路要两端都有人（生产者+消费者），补链路的刀收尾时按「谁生产、谁消费」各数一遍调用者 |
| 一次 Bash 调用里串「本树查 X + 基准查 X」两条命令，看输出就能分清谁是谁（据此两次断定「宿主留着 yukkuri 常量/资源」） | 本树那半是**空输出**，基准的行紧跟其后填了位，被整块误读成本树的；后续用 `git diff ref:path` 复核时命令因 HEAD 路径不存在而失败，空输出又被读成「无差异」——假结论进了提交信息与账本，实机开关一开就崩 | **拼接命令核对存在性时，每条子命令之间必须有显式分隔回显**（`echo ===A===` 式），且「甲有乙无」类结论要用**单命令单树**取证；空输出恒不可作「相同/存在」的证据（证伪表已有条目的我方复发，这次栽在取证阶段而非门禁阶段）。修法之外补了机械闸：注册常量↔资源文件对账（BedrockModelResourceInvariantTest，登记闸家族第三枚） |
| 契约测试断言「标识符出现在 warn 之前」就钉住了守卫；枚举型断言写好就有覆盖 | ① 守卫改成无条件后断言照旧全绿——局部变量赋值本就在方法体更早处 ② 按按钮标签**字面量**识别「哪些屏有检查按钮」，标签换成常量后**静默零覆盖**（靠 `checked > 0` 那道下限断言才照出来） | **「缩到方法体」还不够，要缩到被判定的那个表达式**（取 warn 所属的那个 `if` 条件）。**枚举型断言必须自带「找到了几个看守对象」的下限断言**，否则识别依据一变就静默零覆盖，而零覆盖的测试永远是绿的。识别依据要选语义稳的（「这屏发不发那个包」），别选表现层的（标签文字） |
| 排障中一条自洽因果链失效就换下一条理论（gecko 图标特效入镜：跨帧姿态漂移→同帧双幕→动画未安定→加长等待，三连修症状纹丝不动） | 诊断桩转储 + 动画 json 实查：特效骨骼靠 pre_parallel 动画 scale=0 隐藏，而 CacheScreen 继承了 Screen 默认 `isPauseScreen()=true`——**单人档暂停冻结 level 时间**，一切 `query.anim_time` 驱动的隐藏动画永不推进；时间被冻结时「同帧」「等待」类时间修法全部空转 | **新屏顶替旧屏时，旧屏覆写过的每个 Screen 契约（isPauseScreen 等）都要逐个决定继承与否**——与「复用基类时继承来的每个副作用都得逐个决定」同族，这次的副作用是**全局时间流速**。且症状两连不倒就立刻上诊断桩取第一现场，别再换理论：桩一轮定案，理论换了三轮。另：**基准从未运行过的代码，其注释只是纸面推导**（本簇在 1.21.11 是死代码，「execute 延到下一帧」注释就是错的） |
| 血量变化就是「被我打了」（应战不打断吃东西那条用例断言目标血量不变；另三条断言血量下降） | 探针读出 `lastDamageSource=DamageSource (onFire)`——GameTest 结构在**地表**，僵尸被阳光点燃每秒自燃掉血。那条用例偶发假红（4 轮里红 2 次），而反向的三条「该打」用例更危险：**火烧也能满足「血少了」，它们可能一直是假绿** | **判据要落在「谁造成的」而不是「量变了」。** 环境伤害（自燃、窒息、摔落）与被测行为共用同一个可观测量时，那个量就不再是判据。改用 `getLastHurtByMob`（只记生物攻击者，火不写它）。**同族**：「探针记 result 非 null 就等于有提交」——都是判据分不清两种来源。另：**偶发红必须追到底**，三轮绿不构成「它好了」，第一轮那次红是真的 |
| 测试 fixture 里 `setTarget` 写进去了就读得出来 | `spider.setTarget(owner)` 后 `getTarget()` 恒 null：26.1.2 的 `setTarget` **与** `getTarget` 都经 `asValidTarget` 过滤（javap -c 实证），创造/旁观玩家一律滤成 null；而 GameTest 的 mock 玩家 `setGameMode(SURVIVAL)` **实测无效**（`isCreative()` 恒 true）。更坏的是前两条断言（期望 false）因此**恒真通过**，只有第三条照出来 | **凡「先写入、后读出」的 fixture，必须显式断言写进去了**（`assertSame(expected, actual)`），否则「写入被静默丢弃」会伪装成「逻辑判定为否」，让期望 false 的断言集体假绿。写入方带过滤器是常态（vanilla 尤其），**别把 setter 当赋值**。与「创造模式玩家不被怪物索敌」是同一条 vanilla 规则在**另一个入口**上的体现 |
| 宿主调的 vanilla 方法名对得上，行为就对得上（`Item.hurtEnemy` / `Item.postHurtEnemy`） | 26.1.2 这两个方法的字节码**就是一行 `return`**（javap -c 实证）——耐久已迁到 WEAPON 组件驱动的 `ItemStack.postHurtEnemy`。宿主照旧调 Item 版，于是**女仆近战武器永不损耗**，是一处无人发现的宿主回归 | **空壳化是版本迁移的常见手法：方法还在、签名不变、行为被掏空。** 凡「照抄宿主/基准的调用」，对**行为敏感**的那几个（耐久、伤害、掉落）要 `javap -c` 看一眼方法体是不是 `return`。名字对不代表还干活。本条由新增测试照出，属「宿主有没有把原本就有的东西搁置掉」那一族 |
| 交接情报里的操作事实可以直接用（「lang 文件是带 BOM 的裸 UTF-8 中文，python 用 utf-8-sig 读、原样回写 BOM」） | 实测 17 个 lang 文件**全部无 BOM**（开头就是 `{`）。若照它用 `utf-8-sig` 回写，会给每个文件**凭空加上** BOM | **交接的「操作事实」与它的「结论」一样需要复核**，尤其是这类"照做就会改坏文件"的。判据是**一条命令实测**（`head -c 3 \| od -An -tx1`），成本几秒。该情报多半是从 1.21.11 分支原样搬来的——**与「复制方法论会继承它的前提」同族，只是这次继承的是环境事实** |
| 「同一层的文件都能从行为基准整取，剩下的只是 API 漂移」——`ai/` 包与网络层这么干都成了，GUI 那 18 个屏照办 | 整取后 60+ 个「找不到符号 `GuiGraphics`」：26.1.2 **删掉了整个 `GuiGraphics` 类**，渲染改成 `GuiGraphicsExtractor` + `extractRenderState` 抽取模型，而基准那批屏全是按 `render(GuiGraphics, …)` 写的 | **「能整取」是按子系统成立的，不是按仓库成立的**：判据是**那一层依赖的宿主 API 有没有被重写**，而不是「上一层整取成功了」。可先用一条命令量出来——宿主自己把该层从旧版迁到新版花了多少行（`ai/` 是 +150/−317 故几乎无改写，GUI 是 +224/−245 但**动的是渲染骨架**）。改写发生在骨架上时，正确方向是**把我们的差异铺到宿主已迁好的文件上**，而不是把基准的文件铺过来 |
| 新加的网络包写好、注册处也在同一个文件里，顺手就会记得登记 | 四个新 payload **一个都没登记进 `NetworkHandler`**——编译、打包、启动一路正常，被发送时才踢玩家。同轮还漏了 `@MaidManagerDef`（`EntityMaid.aiChatManager` 整个不存在）与三个 GameTest 的 entrypoint | **静默注册面已实证第 8、9 枚**（前有 mixins.json / fabric-gametest / brain memory 列表 / entrypoint）。规律：**凡「新增的类要被别处的一张表认领才生效」，那张表就是一处静默面**。搬运时的判据不是「代码写完了」，而是「谁认领它、我改过那张表吗」。这一族的通用解法是给每张表配一道机械闸——本轮补的 `PayloadRegistrationInvariantTest` 正是为此而存在，而它此前**也没搬** |
| 契约测试在看管某张表，那张表少一项它就会红 | `SiteSecretRedactionTest` 的字段集**取自 `Site.SECRET_FIELDS` 本身**——把 `SECRET_ID` 从表里删掉，原有 7 条用例一条都不红，只是少测一项，而少的那一项会以明文下行 | **自证式断言**：判据取自被看管对象时，看管等于没有。与「活性判据必须与被断言的量正交」同族，但更隐蔽——那条讲的是「没有违规 ≠ 扫描还活着」，这条讲的是「**清单完整 ≠ 清单看管着自己**」。凡「表 X 必须完整」的断言，判据必须从**独立来源**重建（本例：扫站点源码里声明过的密钥字段，反过来要求表里都有） |
| 知识库/提示词类文本可以照搬全量，多写点没坏处 | 基准的玩法知识文档里有三段描述 §3.E / §3.I，而本分支**没移植那两块**——照搬就等于让女仆言之凿凿地说出这个构建做不到的事 | **喂给模型当事实的文本，只能描述本构建真有的行为**。多写比少写贵：玩家会照着它去试，然后发现她做不到，而那是「说了没做」——一次就摧毁信任。裁剪要与看管它的契约测试同步收窄，并写明恢复条件，否则下一个人会把「缺了」读成「漏了」 |
| 「宿主把方法 A 机械改写成方法 B」意味着 B 是安全的等价物（26.1.2 删 `displayClientMessage`，宿主全树改成 `sendSystemMessage`，我照单接受） | javap -c 实证 `LocalPlayer.sendSystemMessage` 的方法体就是 `getChatListener().handleSystemMessage(component, true)`——**与被禁用的那个是同一条链**，只是换了名字。禁 `displayClientMessage` 的契约测试于是全绿地放过了三处真实阻塞（聊天屏玩家回显 + `TTSPlayer2Client` 两处） | **禁止型契约的判据要按「成因」写，不是按「某个方法名的写法」写**。成因是「谁把文本送进了 ChatListener」，所以版本迁移换掉方法名时判据必须跟着走。判法：对新名字 `javap -c` 看方法体，别看方法名像不像。同族：「空壳化是版本迁移的常见手法」——那条讲行为被掏空，这条讲**行为被换了个入口**，两者都要求读字节码而不是读签名 |
| 契约测试的扫描面按「这个功能住在哪个包」划就够了（`ClientLocalChatContractTest` 的 SCOPE 只有 `client/sound` 与 `ai/**`） | 同一条阻塞链在 `client/gui` 里也有一处（聊天屏回显玩家自己那行，格式 `<玩家名> 文本` 正好命中 `guessChatUUID`），SCOPE 根本够不着，白绿了整整一轮 | **扫描面要按「谁可能犯这个错」划，不是按「这个功能属于谁」划**。判据：这条链的调用者可能出现在哪些层？UI 层几乎总在其中，而它最容易被漏——因为写契约的人当时正在改的是服务层。与「扫描的判据要按问题的成因维度定」同源，这次栽在**范围**而不是**模式**上 |
| 交接清单列出的「本刀新建三件」都是真需要新建的 | `MaidAIChatConfigButton` 在 `origin/1.21.1` 与行为基准 **都是零调用者的死代码**（全仓 `git grep` 三个 ref、不限 `src/main` 实证；贴图 `maid_ai_chat_config.png` 的真实消费者是另外三个类）。照单新建就是凭空造一个空壳 | **「基准有这个文件」不等于「基准用这个文件」。** 凡清单说「新建/补回 X」，动手前先数 X 在**基准里**的调用者——零调用者就是死代码，补它等于把别人的技术债搬过来。这与「不留零消费者的空壳」是同一条规则的**上游**：那条管我不要新造，这条管我不要照搬别人造好的 |
| 红测脚本写成「注入」与「还原」互为反向替换就够了 | 删除型注入的还原是「把空串换回原文」，而空串在文件里匹配上万次——还原当场崩，五个文件停在半注入状态。所幸随后 `git diff` 逐条核对过「未提交改动恰好只有这 5 处注入」才敢 `git checkout` | **可逆操作要验证它真的可逆，别假定对称。** 红测的正确形态是：注入前确认工作树干净（或改动已提交），还原一律走 `git checkout --`，而不是自己写反向替换。另：注入前必须先跑一次编译——本轮缺陷④（摘掉 `implements` 却留着 `@Override`）**编译就过不了**，白跑一轮 |
| 把配置对象的读法写成方法引用与写成 lambda 是一回事 | `AIConfig.STT_PROXY_ADDRESS::get` 在**创建那一刻**就解引用字段，于是 `STTSite` 的静态初始化依赖「spec 已建好」；纯 JUnit 下 `NoClassDefFoundError`，且**类初始化失败会被缓存**，后续所有用例一起垮，看起来像我刚改的那处逻辑把一切弄坏了 | **`X::get` 求值 X，`() -> X.get()` 不求值 X。** 凡在静态初始化里引用「别处初始化的可变字段」，一律用 lambda 延迟到调用时。另：**类初始化失败会被 JVM 缓存**，所以这类故障的爆炸半径与首个触发者有关而与真正的错处无关——看到一片 `NoClassDefFoundError: Could not initialize class` 先去找那个类的静态块，别顺着最新改动查 |

## 🧭 工程方法论（PERMANENT——未来会话遵此，勿重新发现）

### 四方校验（每次非平凡改动必做）
| 来源 | 角色 |
|---|---|
| **`port/1.21.11-fabric` 分支** | **行为基准（唯一）**——那份已经用户实机验收过的实现。26.1.2 的目标是与它行为一致，不是重新推导 |
| `origin/26.1` | **代码宿主**：新基的结构、命名与写法照它，**行为不照它** |
| `origin/1.21.1` | 原始行为基准；1.21.11 分支与它的差异见 `docs/PORT_26X_AUDIT.md` §3 |
| javap（Mojang mappings jar）+ 反编译源 | **API 行为最终裁判** |

⚠️ **`origin/1.21.1` 与 `origin/26.x` 无共同祖先**，`cherry-pick` / `rebase` / `format-patch` 全部无效。
差异化只能按行为重做，见 `docs/PORT_26X_AUDIT.md` §5。

### 核心纪律
1. **运行时正确性 > 编译错误数**（错误数是虚荣指标；javac 抓不到的持久化/mixin/注册炸弹才是真闸门）。compile==0 ≠ 正确；boot 通过 ≠ 正确；**非默认路径只有入世实测能验**。
2. **忠实移植 —— 写法对新基，行为对 1.21.11**：禁止 stub/占位/为编译改行为/擅自"现代化"（R5/R6-3 教训：无人强迫的改动=一半的 bug 来源）。代码可以重 derive，**行为不许变**。
   ⚠️ **原文此处禁止「为贴近 26.1 改行为」，方向是反的，已据本分支修正**：那句写于 1.21.11 语境，
   那时 `origin/26.1` 只是**架构参照**；本分支它是**代码宿主**，新基的写法就是 26.1.2 的正确写法。
   判据拆成两问：**这段代码长什么样 → 照 `origin/26.1`；它表现成什么样 → 照 `port/1.21.11-fabric`。**
   看到新基与 1.21.11 写法不同，**默认新基是对的**，除非能证明行为因此变了。
3. **新增的类 = 静默隐形 · 落地 = 两查**：**延后理由要定期重验**（R7 教训：理由早已消除，只差一行接线）。**接线修复必须验到链路终点**（init 被调 ≠ init 有内容；同一延后锚点可能分布多文件）。
   ⚠️ **原文的「排除 = 隐形」前提在本分支不存在，已据实查修正**：新基 `build.gradle` 的
   `sourceSets.main.java.exclude` **是 0 条**（那批排除行是 1.21.11 逐个解除排除时的产物）。
   但**三查里的后两查对「我们新增的类」照旧有效**，且本轮已实证（GameTest 类不登记 entrypoint 就永不执行）：
   **凡我们新增的类必查两处，缺一即"纸面接口"（已实证 7 枚）**：① **`fabric.mod.json` entrypoint**（Fabric 的 JEI/Jade/EMI/REI 靠它发现插件——`@JeiPlugin`/`@WailaPlugin` 是 **NeoForge** 机制，在 Fabric 上是死注解；**GameTest 类同理，靠 `fabric-gametest` entrypoint 发现**）② `*.mixins.json` 条目。**两查全静默**——编译、打包、启动一路正常，功能从不执行，没有任何一步会自己报错。（原文的①「`build.gradle` 排除行」与「五项恢复闸门详见 `docs/COMPAT.md`」一并删去：前者本分支无适用对象，后者那份文档没有随方法论迁进本分支。）
   **凡"先移除、承诺后补"的操作，恢复锚点必须覆盖被移除项的全集**，否则漏项无人认领（entrypoint 事故根因：`P5_CLIENT_RUNCONFIG_RESTORE` 只覆盖了 client entrypoint，同期移除的 4 个 compat entrypoint 无锚点）。
4. **依赖优先 + frozen-node**（⚠️ **本分支暂无适用对象，保留备查**：这是 1.21.11 上「逐个解除源码排除」时的依赖排序法，而新基零条排除，见第 3 条。做 26.2 时可能复用，故不删）：已完成节点=可信基础设施勿重开；依赖序测一次即冻结，仅当编译器证无关子系统入闭包才重测。SCC 不可分解时 measurement-pass（apply→测→revert 保 clean），永不 commit 半成。
5. **绝不按方法名全局替换**——先确认接收者类型。每 tick 的 vanilla 内部钩子（handlePortal/baseTick 族）绝不能承载一次性事件逻辑——嫁接前必查调用频率（R6 教训）。
6. **症状即证据**：从运行时表象反查 API（末影人粒子=broadcastEntityEvent(46)=randomTeleport）比全面 diff 更快。注释也是一等审计对象（TODO 事实断言可能是错的；「延后=安全」声明要复核）。
   **诊断桩定案法**：两树逐字对照到头、症状仍不可解时 → 插临时 `[TLM-QA-*]` 日志桩 → 门实例或用户操作触发 → 读日志定案 → **桩用完即删**（本轮两次均一次命中）。
   **先分数据层 / 显示层**：「东西不见了」未必是数据缺失。2026-07-20 三例全是显示层（JEI 按 uid 折叠变体 / lang 颜色缺 Alpha 全透明 / entrypoint 惰性致插件从未运行），数据层始终完好——**归因前先用探针数一下真实数量**。
7. **每节点收尾** = compile0 + runClient 干净 + 文档同步 + commit + **用户入世验证**；文档是 definition-of-done 的一部分。
8. 仓库约定 **`blockentity/BlockEntity*`** 命名（⚠️ 原文写的是 `tileentity/TileEntity*`，那是 1.21.11 的约定，**在本分支是假的**：新基已改名，祭坛叫 `BlockEntityAltar`，全树无 `tileentity` 目录）；硬依赖只有 Fabric API + Forge Config API Port（**经查仍成立**：`fabric.mod.json` 的 `depends` 恰为 `fabric-api` / `forgeconfigapiport` / `minecraft`），其余全 isModLoaded 软兼容，JiJ=0（**经查仍成立**：`build.gradle` 零个生效的 `include(`）。
9. 遇过时依赖/找不到仓库 → 直接问用户要 repo。
10. **测试流程**：runClient 由用户自启自退（PowerShell `Start-Process` 脱离启动或用户 `! ./gradlew runClient`），**勿 timeout 击杀**；每轮给用户测试清单+问答。
    **自动化门实例**（不占用用户）：`--args="--quickPlaySingleplayer \"<存档名>\""` 直接入世，比只到标题界面多覆盖注册/渲染/AI/创造栏路径；用后台监视器等「入世成功 / mixin apply 失败 / 崩溃」三态（**监视器必须覆盖失败形态，否则崩溃与运行中都表现为静默**），命中后脚本自动 `Stop-Process` 关闭——**验完必关，别留挂**。
11. **1.21.11 起的陷阱备忘——26.1.2 继承，但每条动到时都要在新版本上复核一次**（原文：**客户端不再持有 `RecipeManager`**——自定义 `RecipeType` 不同步客户端，凡客户端枚举配方处走 `Minecraft.getSingleplayerServer().getRecipeManager().getRecipes()` 过滤，专服需自定义同步；**JEI 27 变体区分**用 `registerFromDataComponentTypes`，旧 `ISubtypeInterpreter` 通道不生效）：`CompoundTag.read(fieldOf)` 缺键自动记 ERROR（可缺键用 Optional getter）；SavedDataType id=存档文件名（带冒号必炸 Windows）；mcmeta 校验对 format 65..81 自相矛盾（mod 包保持 origin 旧值）；`ItemStack.CODEC` 需 `provider.createSerializationContext(NbtOps)`；**9 参 legacy `blit(Identifier,…)` 真实语义 = `(x0,y0,x1绝对,y1绝对,u0,u1,v0,v1)`**——绝不当 (x,y,w,h,uv) 用，一律用 `blit(RenderPipelines.GUI_TEXTURED, tex, x, y, u, v, w, h, texW, texH)`；**`Screen.renderWithTooltipAndSubtitles`（final）已在 render() 前强制调 renderBackground（含 blur）**——屏内再调背景 API = "Can only blur once per frame" 崩溃（恢复排除屏时必查此 idiom）；**mixin/accessor 类编译 ≠ 生效**——必须查 mixins.json 注册（已抓 5 枚"纸面接口"：ScreenAccessor、LivingEntityRendererMixin、KeyboardHandlerMixin、SoundEngineMixin、ChannelAccessHandleMixin；症状=CCE 或功能静默失效，origin fabric client 数组 17 项是核对基准）；**注册 mixin 前 javap 验"靶点存在"不够——目标方法返回类型变了（void→非 void）则 `@Inject` 回调必须 `CallbackInfoReturnable`，否则 apply 失败、required 下启动崩**（SoundEngineMixin.play 实证），启动门必须真跑；**`TextureManager.registerAndLoad` 立即上传 GL、必须渲染线程**（1.21.1 `register` 延迟上传+recordRenderCall 均已删）——任何 worker 线程的纹理注册须 `Minecraft.execute` 分派（R13/C07 两次实证）。

### 编译错误测量（务必遵守）
```bash
./gradlew compileJava --console=plain --no-daemon -I <scratch>/maxerrs.gradle > log.txt 2>&1
grep -E '^[0-9]+ 个错误' log.txt | head -1     # 唯一权威 = javac 自报
```
maxerrs.gradle（勿改 build.gradle）在 Gradle 9 下**必须包 `allprojects`**，否则报
`Could not get unknown property 'tasks'`：
```groovy
allprojects { tasks.withType(JavaCompile).configureEach { options.compilerArgs << '-Xmaxerrs' << '100000' } }
```。日志 UTF-8；Python 须设 `PYTHONIOENCODING=utf-8`；含 `\` 的脚本用 Write 工具建（Bash heredoc 吃反斜杠）。

### 常用命令
```bash
git diff --ignore-cr-at-eol origin/1.21.1 -- <file>    # 行为基准对照
git show origin/26.1:<path>                             # 架构参考
javap -p -cp <mojang-mapped-mc.jar> <fqcn>              # loom.mappings.* 那个 jar，不是 yarn
```


## 当前状态

**唯一状态账本 = `docs/CURRENT_STATUS.md`；移植范围与边界 = `docs/PORT_26X_AUDIT.md`。**
本文件不记录状态。

## 版本配置

| 组件 | 值 |
|---|---|
| Minecraft | 26.1.2 · Fabric Loader 0.19.2 |
| Fabric API | 0.155.2+26.1.2 |
| Forge Config API Port | 26.1.5（**硬依赖**） |
| 依赖档位 | **允许 beta / alpha**（JEI 只有 beta、Sodium 只有 alpha、Patchouli 只有 beta），用户 2026-08-13 批准 |

完整前置矩阵（含加载器粒度的实查结果）见 `docs/PORT_26X_AUDIT.md` §2。

## 关键文件

| 文件 | 用途 |
|---|---|
| `docs/CURRENT_STATUS.md` | ⭐ 唯一状态账本与待办来源；每次接手必读 |
| `docs/PORT_26X_AUDIT.md` | 差异化全集、移植边界与完整性论证 |
| `docs/HANDOFF.md` | 分支、工作树与隔离约定 |
| `docs/tools/` | 门禁与测试工具（自 1.21.11 分支带入的副本） |
| `build.gradle` | 已加入 JUnit、`runGametest`、`clientDedicated` |
| `fabric.mod.json` | entrypoint 总表；**新增 GameTest 类必须登记 `fabric-gametest`，否则永不执行** |
